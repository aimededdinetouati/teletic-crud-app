/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Pageable } from '../../models/pageable';
import { PageTaskDto } from '../../models/page-task-dto';

export interface GetUserTasks$Params {

/**
 * Filter by status (optional)
 */
  status?: 'TO_DO' | 'IN_PROGRESS' | 'COMPLETED' | 'CANCELLED';

/**
 * Pagination parameters
 */
  pageable: Pageable;
}

export function getUserTasks(http: HttpClient, rootUrl: string, params: GetUserTasks$Params, context?: HttpContext): Observable<StrictHttpResponse<PageTaskDto>> {
  const rb = new RequestBuilder(rootUrl, getUserTasks.PATH, 'get');
  if (params) {
    rb.query('status', params.status, {});
    rb.query('pageable', params.pageable, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PageTaskDto>;
    })
  );
}

getUserTasks.PATH = '/tasks/my';
